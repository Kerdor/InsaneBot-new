# Research Log — `python-discord/bot`

Статус: 🔵 **АКТИВЕН**

Правило: этот репозиторий должен быть полностью обработан до перехода к следующему источнику.

## Порядок обхода

Формат статуса:
- `⏳` — ещё не обработано
- `🔄` — сейчас исследуется
- `✅` — полностью просмотрено, идеи сверены с `ideas/`, новые механики распределены
- `➖` — просмотрено, новых переносимых механик не найдено

### Корень репозитория

- ⏳ Дерево репозитория
- ⏳ README / документация корня
- ⏳ Конфигурационные и служебные файлы корня

### Директории

> Ниже будут добавляться **все найденные папки и файлы в порядке фактического обхода**. Ничего не пропускаем только потому, что файл кажется очевидным или второстепенным.

Пока не начат обход исходного репозитория.

## Последняя обработанная точка

**Ещё не начато.**

## Следующий файл/папка

Начать с корня репозитория: дерево → README/документация → конфигурация → исходный код → тесты → CI/deployment и остальные поверхности по фактической структуре.

## Найденные новые механики

Пока нет.

## Примечания для продолжения

- Не переходить к `ItzSudhan/Discord-MusicBot`, пока этот документ не будет помечен как `ЗАВЕРШЁН`.
- Перед каждой новой записью сверять найденное с существующим банком идей.
- После существенного батча обновлять этот журнал и `PROJECT_STATE.md`.
